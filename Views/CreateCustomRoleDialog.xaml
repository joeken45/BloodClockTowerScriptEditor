<Window x:Class="BloodClockTowerScriptEditor.Views.CreateCustomRoleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:BloodClockTowerScriptEditor.Converters"
        Title="新增自訂角色" 
        Height="700" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>
        <converters:TeamTypeConverter x:Key="TeamTypeConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 表單區 -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">

                <!-- 標題 -->
                <TextBlock Text="角色編輯" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Margin="0,0,0,8"/>
                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,0,0,12"/>

                <!-- 第一行: 角色 ID + 角色名稱 -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DockPanel Grid.Column="0">
                        <TextBlock Text="角色 ID *" FontWeight="SemiBold" DockPanel.Dock="Left" 
                                   VerticalAlignment="Center" Margin="0,0,8,0" Width="80"/>
                        <TextBox x:Name="txtId" 
                                 Padding="6,4" 
                                 BorderBrush="#CCC" 
                                 BorderThickness="1" 
                                 VerticalAlignment="Center"
                                 TextChanged="ValidateForm"/>
                    </DockPanel>

                    <DockPanel Grid.Column="2">
                        <TextBlock Text="角色名稱 *" FontWeight="SemiBold" DockPanel.Dock="Left" 
                                   VerticalAlignment="Center" Margin="0,0,8,0" Width="80"/>
                        <TextBox x:Name="txtName" 
                                 Padding="6,4" 
                                 BorderBrush="#CCC" 
                                 BorderThickness="1" 
                                 VerticalAlignment="Center"
                                 TextChanged="ValidateForm"/>
                    </DockPanel>
                </Grid>

                <!-- 第二行: 角色類型 + 英文名稱 -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DockPanel Grid.Column="0">
                        <TextBlock Text="角色類型 *" FontWeight="SemiBold" DockPanel.Dock="Left" 
                                   VerticalAlignment="Center" Margin="0,0,8,0" Width="80"/>
                        <ComboBox x:Name="cmbTeam" 
                                  Padding="6,4" 
                                  BorderBrush="#CCC" 
                                  BorderThickness="1" 
                                  VerticalAlignment="Center"
                                  SelectedIndex="0"
                                  SelectionChanged="ValidateForm">
                            <ComboBoxItem Content="鎮民" Tag="townsfolk"/>
                            <ComboBoxItem Content="外來者" Tag="outsider"/>
                            <ComboBoxItem Content="爪牙" Tag="minion"/>
                            <ComboBoxItem Content="惡魔" Tag="demon"/>
                            <ComboBoxItem Content="旅行者" Tag="traveler"/>
                            <ComboBoxItem Content="傳奇" Tag="fabled"/>
                            <ComboBoxItem Content="奇遇" Tag="loric"/>
                        </ComboBox>
                    </DockPanel>

                </Grid>

                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,8"/>

                <!-- 第三行: 圖片 URL + 預覽 -->
                <Grid Margin="0,8,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               Text="圖片 URL" 
                               FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Column="2" 
                             x:Name="txtImage" 
                             Padding="6,4" 
                             BorderBrush="#CCC" 
                             BorderThickness="1" 
                             VerticalAlignment="Center"
                             TextChanged="ImageUrl_TextChanged"/>

                    <Border Grid.Column="4" 
                            Width="50" 
                            Height="50" 
                            BorderBrush="#CCC" 
                            BorderThickness="1"
                            Background="#F9F9F9">
                        <Image x:Name="imgPreview" 
                               Stretch="Uniform"/>
                    </Border>
                </Grid>

                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,8"/>

                <!-- 能力描述 -->
                <DockPanel Margin="0,8,0,8">
                    <TextBlock Text="能力 *" 
                               FontWeight="SemiBold" 
                               DockPanel.Dock="Left" 
                               VerticalAlignment="Top" 
                               Margin="0,4,8,0" 
                               Width="80"/>
                    <TextBox x:Name="txtAbility" 
                             TextWrapping="Wrap" 
                             AcceptsReturn="True" 
                             Height="80"
                             Padding="6,4" 
                             BorderBrush="#CCC" 
                             BorderThickness="1" 
                             FontSize="11" 
                             VerticalScrollBarVisibility="Auto"
                             TextChanged="ValidateForm"/>
                </DockPanel>

                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,8"/>

                <!-- 所屬劇本 + 影響設置 -->
                <Grid Margin="0,8,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               Text="所屬劇本" 
                               FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Column="2" 
                             x:Name="txtEdition" 
                             Text="custom"
                             Padding="6,4" 
                             BorderBrush="#CCC" 
                             BorderThickness="1" 
                             VerticalAlignment="Center"/>

                    <CheckBox Grid.Column="3" 
                              x:Name="chkSetup" 
                              Content="影響劇本設置" 
                              VerticalAlignment="Center" 
                              Margin="12,0,0,0"/>
                </Grid>

                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,8"/>

                <!-- 夜晚順序 + 提示 -->
                <Grid Margin="0,8,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左側：首夜 -->
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0">
                            <TextBlock Text="首夜順序" 
                                       FontWeight="SemiBold" 
                                       DockPanel.Dock="Left" 
                                       VerticalAlignment="Center" 
                                       Margin="0,0,8,0" 
                                       Width="70"/>
                            <TextBox x:Name="txtFirstNight" 
                                     Text="0"
                                     Padding="6,4" 
                                     BorderBrush="#CCC" 
                                     BorderThickness="1" 
                                     VerticalAlignment="Center"/>
                        </DockPanel>

                        <DockPanel Grid.Row="2">
                            <TextBlock Text="首夜提示" 
                                       FontWeight="SemiBold" 
                                       DockPanel.Dock="Left" 
                                       VerticalAlignment="Top" 
                                       Margin="0,4,8,0" 
                                       Width="70"/>
                            <TextBox x:Name="txtFirstNightReminder" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True" 
                                     Height="80"
                                     Padding="6,4" 
                                     BorderBrush="#CCC" 
                                     BorderThickness="1" 
                                     FontSize="11" 
                                     VerticalScrollBarVisibility="Auto"/>
                        </DockPanel>
                    </Grid>

                    <!-- 右側：其他夜 -->
                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0">
                            <TextBlock Text="其他夜順序" 
                                       FontWeight="SemiBold" 
                                       DockPanel.Dock="Left" 
                                       VerticalAlignment="Center" 
                                       Margin="0,0,8,0" 
                                       Width="80"/>
                            <TextBox x:Name="txtOtherNight" 
                                     Text="0"
                                     Padding="6,4" 
                                     BorderBrush="#CCC" 
                                     BorderThickness="1" 
                                     VerticalAlignment="Center"/>
                        </DockPanel>

                        <DockPanel Grid.Row="2">
                            <TextBlock Text="其他夜提示" 
                                       FontWeight="SemiBold" 
                                       DockPanel.Dock="Left" 
                                       VerticalAlignment="Top" 
                                       Margin="0,4,8,0" 
                                       Width="80"/>
                            <TextBox x:Name="txtOtherNightReminder" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True" 
                                     Height="80"
                                     Padding="6,4" 
                                     BorderBrush="#CCC" 
                                     BorderThickness="1" 
                                     FontSize="11" 
                                     VerticalScrollBarVisibility="Auto"/>
                        </DockPanel>
                    </Grid>
                </Grid>

                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,8"/>

                <!-- 提示標記 -->
                <TextBlock Text="提示標記" FontWeight="SemiBold" Margin="0,8,0,8"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左側：一般標記 -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="一般標記" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <Button Content="➕ 新增" Padding="6,3" FontSize="11"
            Click="ManageReminder_Click" Tag="Add|Normal"
            Background="#4CAF50" Foreground="White" BorderThickness="0" Margin="0,0,6,0"/>
                            <Button Content="🗑️ 刪除" Padding="6,3" FontSize="11"
            Click="ManageReminder_Click" Tag="Remove|Normal"
            Background="#F44336" Foreground="White" BorderThickness="0"/>
                        </StackPanel>

                        <Border BorderBrush="#DDD" BorderThickness="1" Height="100" Background="White">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="remindersList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="6,4" Background="White">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="6"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <CheckBox Grid.Column="0" 
                                                              IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                                              VerticalAlignment="Center"/>

                                                    <TextBox Grid.Column="2" 
                                                             Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" 
                                                             BorderThickness="0" 
                                                             Background="Transparent"
                                                             Padding="4,2"
                                                             FontSize="11"
                                                             VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>

                    <!-- 右側：全局標記 -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="全局標記" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <Button Content="➕ 新增" Padding="6,3" FontSize="11"
            Click="ManageReminder_Click" Tag="Add|Global"
            Background="#4CAF50" Foreground="White" BorderThickness="0" Margin="0,0,6,0"/>
                            <Button Content="🗑️ 刪除" Padding="6,3" FontSize="11"
            Click="ManageReminder_Click" Tag="Remove|Global"
            Background="#F44336" Foreground="White" BorderThickness="0"/>
                        </StackPanel>

                        <Border BorderBrush="#DDD" BorderThickness="1" Height="100" Background="White">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="globalRemindersList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="6,4" Background="White">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="6"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <CheckBox Grid.Column="0" 
                                                              IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                                              VerticalAlignment="Center"/>

                                                    <TextBox Grid.Column="2" 
                                                             Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" 
                                                             BorderThickness="0" 
                                                             Background="Transparent"
                                                             Padding="4,2"
                                                             FontSize="11"
                                                             VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Grid>

                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,10"/>

                <!-- ⭐ 特殊功能編輯區塊 -->
                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,8"/>

                <TextBlock Text="⭐ 特殊功能 (Special)" 
                           FontWeight="SemiBold" 
                           FontSize="13" 
                           Margin="0,8,0,8"/>

                <Button Content="➕ 新增特殊功能" 
                        Click="AddSpecial_Click"
                        Margin="0,0,0,8" 
                        Padding="8,4"
                        HorizontalAlignment="Left"
                        Background="#2196F3" 
                        Foreground="White" 
                        BorderThickness="0"/>

                <ItemsControl x:Name="specialItemsControl" Margin="0,0,0,8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4"
                                    Margin="0,0,0,8" Padding="10" Background="#FAFAFA">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="35"/>
                                    </Grid.ColumnDefinitions>

                                    <Grid Grid.Row="0" Grid.Column="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- 第一行：Type 和 Name -->
                                        <Grid Grid.Row="0" Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <DockPanel Grid.Column="0">
                                                <TextBlock Text="觸發時機" Width="65" DockPanel.Dock="Left"
                                                           VerticalAlignment="Center" FontWeight="SemiBold"
                                                           FontSize="11" Margin="0,0,6,0"/>
                                                <ComboBox SelectedValue="{Binding Type, UpdateSourceTrigger=PropertyChanged}"
                                                          SelectedValuePath="Tag" Padding="6,3"
                                                          BorderBrush="#CCC" FontSize="11">
                                                    <ComboBoxItem Content="角色選擇時 (selection)" Tag="selection"/>
                                                    <ComboBoxItem Content="點擊標記時 (ability)" Tag="ability"/>
                                                    <ComboBoxItem Content="夜晚訊號時 (signal)" Tag="signal"/>
                                                    <ComboBoxItem Content="投票時 (vote)" Tag="vote"/>
                                                    <ComboBoxItem Content="遊戲結束前 (reveal)" Tag="reveal"/>
                                                    <ComboBoxItem Content="玩家主動觸發 (player)" Tag="player"/>
                                                    <ComboBoxItem Content="標記 (reminder)" Tag="reminder"/>
                                                </ComboBox>
                                            </DockPanel>

                                            <DockPanel Grid.Column="2">
                                                <TextBlock Text="功能類型" Width="65" DockPanel.Dock="Left"
                                                           VerticalAlignment="Center" FontWeight="SemiBold"
                                                           FontSize="11" Margin="0,0,6,0"/>
                                                <ComboBox SelectedValue="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                                          SelectedValuePath="Tag" Padding="6,3"
                                                          BorderBrush="#CCC" FontSize="11">
                                                    <ComboBoxItem Content="魔典相關 (grimoire)" Tag="grimoire"/>
                                                    <ComboBoxItem Content="指定玩家 (pointing)" Tag="pointing"/>
                                                    <ComboBoxItem Content="鬼魂投票 (ghost-votes)" Tag="ghost-votes"/>
                                                    <ComboBoxItem Content="分發角色 (distribute-roles)" Tag="distribute-roles"/>
                                                    <ComboBoxItem Content="禁用抽取 (bag-disabled)" Tag="bag-disabled"/>
                                                    <ComboBoxItem Content="可重複 (bag-duplicate)" Tag="bag-duplicate"/>
                                                    <ComboBoxItem Content="倍數效果 (multiplier)" Tag="multiplier"/>
                                                    <ComboBoxItem Content="隱藏 (hidden)" Tag="hidden"/>
                                                    <ComboBoxItem Content="替換角色 (replace-character)" Tag="replace-character"/>
                                                    <ComboBoxItem Content="玩家能力 (player)" Tag="player"/>
                                                    <ComboBoxItem Content="卡片提示 (card)" Tag="card"/>
                                                    <ComboBoxItem Content="睜眼 (open-eyes)" Tag="open-eyes"/>
                                                    <ComboBoxItem Content="公開 (public)" Tag="public"/>
                                                </ComboBox>
                                            </DockPanel>
                                        </Grid>

                                        <!-- 第二行：Value、Time、Global -->
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <DockPanel Grid.Column="0">
                                                <TextBlock Text="參數值" Width="50" DockPanel.Dock="Left"
                                                           VerticalAlignment="Center" FontSize="11"
                                                           Foreground="#666" Margin="0,0,6,0"/>
                                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                         Padding="4,2" BorderBrush="#CCC" BorderThickness="1" FontSize="11"/>
                                            </DockPanel>

                                            <DockPanel Grid.Column="2">
                                                <TextBlock Text="階段" Width="40" DockPanel.Dock="Left"
                                                           VerticalAlignment="Center" FontSize="11"
                                                           Foreground="#666" Margin="0,0,6,0"/>
                                                <ComboBox SelectedValue="{Binding Time, UpdateSourceTrigger=PropertyChanged}"
                                                          SelectedValuePath="Tag" Padding="4,2"
                                                          BorderBrush="#CCC" FontSize="11">
                                                    <ComboBoxItem Content="(無)" Tag=""/>
                                                    <ComboBoxItem Content="遊戲前 (pregame)" Tag="pregame"/>
                                                    <ComboBoxItem Content="白天 (day)" Tag="day"/>
                                                    <ComboBoxItem Content="夜晚 (night)" Tag="night"/>
                                                    <ComboBoxItem Content="首夜 (firstNight)" Tag="firstNight"/>
                                                    <ComboBoxItem Content="首日 (firstDay)" Tag="firstDay"/>
                                                    <ComboBoxItem Content="其他夜 (otherNight)" Tag="otherNight"/>
                                                    <ComboBoxItem Content="其他日 (otherDay)" Tag="otherDay"/>
                                                </ComboBox>
                                            </DockPanel>

                                            <DockPanel Grid.Column="4">
                                                <TextBlock Text="範圍" Width="40" DockPanel.Dock="Left"
                                                           VerticalAlignment="Center" FontSize="11"
                                                           Foreground="#666" Margin="0,0,6,0"/>
                                                <ComboBox SelectedValue="{Binding Global, UpdateSourceTrigger=PropertyChanged}"
                                                          SelectedValuePath="Tag" Padding="4,2"
                                                          BorderBrush="#CCC" FontSize="11">
                                                    <ComboBoxItem Content="(無)" Tag=""/>
                                                    <ComboBoxItem Content="鎮民 (townsfolk)" Tag="townsfolk"/>
                                                    <ComboBoxItem Content="外來者 (outsider)" Tag="outsider"/>
                                                    <ComboBoxItem Content="爪牙 (minion)" Tag="minion"/>
                                                    <ComboBoxItem Content="惡魔 (demon)" Tag="demon"/>
                                                    <ComboBoxItem Content="旅行者 (traveller)" Tag="traveller"/>
                                                    <ComboBoxItem Content="死者 (dead)" Tag="dead"/>
                                                </ComboBox>
                                            </DockPanel>
                                        </Grid>
                                    </Grid>

                                    <!-- 刪除按鈕 -->
                                    <Button Grid.Row="0" Grid.Column="1"
                                            Content="🗑️" Click="DeleteSpecial_Click" Tag="{Binding}"
                                            VerticalAlignment="Center" Padding="6,4" Width="30" Height="28"
                                            Background="#FFEBEE" Foreground="#C62828"
                                            BorderBrush="#E57373" BorderThickness="1" ToolTip="刪除" FontSize="11"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- 備註 -->
                <DockPanel Margin="0,8,0,0">
                    <TextBlock Text="備註" 
                               FontWeight="SemiBold" 
                               DockPanel.Dock="Left" 
                               VerticalAlignment="Top" 
                               Margin="0,4,8,0" 
                               Width="80"/>
                    <TextBox x:Name="txtFlavor" 
                             TextWrapping="Wrap" 
                             AcceptsReturn="True" 
                             Height="60"
                             Padding="6,4" 
                             BorderBrush="#CCC" 
                             BorderThickness="1" 
                             FontSize="11" 
                             VerticalScrollBarVisibility="Auto"/>
                </DockPanel>

            </StackPanel>
        </ScrollViewer>

        <!-- 底部按鈕 -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                Padding="20,15" 
                BorderBrush="#DDD" 
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnSave"
                        Content="💾 儲存到資料庫" 
                        Width="120" 
                        Height="35"
                        Background="#4CAF50" 
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Margin="0,0,10,0"
                        Click="Save_Click"
                        IsEnabled="False"/>

                <Button Content="取消" 
                        Width="80" 
                        Height="35"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>