<Window x:Class="BloodClockTowerScriptEditor.Views.EditScriptMetaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="編輯劇本資訊"
        Height="800" Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 編輯區域 -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 標題 -->
                <TextBlock Text="編輯劇本資訊" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                <!-- ========== 基本資訊區 ========== -->
                <TextBlock Text="基本資訊" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                <!-- 劇本名稱 + 作者 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="劇本名稱:" FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="txtName" Padding="6" 
                             VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="3" Text="作者:" FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Column="4" x:Name="txtAuthor" Padding="6" 
                             VerticalAlignment="Center"/>
                </Grid>

                <!-- Logo URL -->
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Logo URL:" FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="txtLogo" Padding="6" 
                             VerticalAlignment="Center"/>
                </Grid>

                <!-- Logo 預覽 -->
                <Border BorderBrush="#CCC" BorderThickness="1" Background="White" 
                        Height="100" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12">
                    <Image x:Name="imgLogo" Stretch="Uniform" Margin="4"/>
                </Border>

                <!-- ========== BOTC 擴充欄位區 ========== -->
                <Separator Margin="0,10,0,15"/>
                <TextBlock Text="BOTC 擴充欄位（可選）" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                <!-- 隱藏標題 -->
                <CheckBox x:Name="chkHideTitle" Content="隱藏劇本標題與作者" 
                          Margin="0,0,0,12" FontSize="13"/>

                <!-- 背景圖片 URL -->
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="背景圖片 URL:" FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="txtBackground" Padding="6" 
                             VerticalAlignment="Center"/>
                </Grid>

                <!-- 背景圖片預覽 -->
                <Border BorderBrush="#CCC" BorderThickness="1" Background="White" 
                        Height="100" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12">
                    <Image x:Name="imgBackground" Stretch="Uniform" Margin="4"/>
                </Border>

                <!-- 說明書連結 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="說明書連結:" FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="txtAlmanac" Padding="6" 
                             VerticalAlignment="Center"/>
                </Grid>

                <!-- 私貨商人規則 -->
                <Separator Margin="0,10,0,15"/>
                <TextBlock Text="私貨商人規則" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button Content="➕ 新增規則" Click="AddBootlegger_Click" 
                                Padding="8,4" Margin="0,0,8,0" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                        <Button Content="🗑️ 刪除選取" Click="DeleteSelectedBootlegger_Click" 
                                Padding="8,4" Background="#F44336" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </Grid>

                <Border BorderBrush="#CCC" BorderThickness="1" Background="White" Height="150" Margin="0,0,0,12">
                    <ListView x:Name="bootleggerList" 
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              SelectionMode="Multiple">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Top" Margin="0,2,0,0"/>
                                    <TextBox Grid.Column="1" Text="{Binding Rule, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                             TextWrapping="Wrap" AcceptsReturn="True" 
                                             BorderThickness="0" Background="Transparent" Padding="4,0" MinHeight="24"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>

                <!-- ========== 狀態區 ========== -->
                <Separator Margin="0,10,0,15"/>
                <TextBlock Text="狀態列表" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button Content="➕ 新增狀態" Click="AddStatus_Click" 
                                Padding="8,4" Margin="0,0,8,0" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                        <Button Content="🗑️ 刪除選取" Click="DeleteSelectedStatus_Click" 
                                Padding="8,4" Background="#F44336" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </Grid>

                <Border BorderBrush="#CCC" BorderThickness="1" Background="White" Height="150" Margin="0,0,0,12">
                    <ListView x:Name="statusList" 
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              SelectionMode="Multiple">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Text="{Binding Name, Mode=TwoWay}" 
                                             Margin="4,0" Padding="4" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="2" Text="{Binding Skill, Mode=TwoWay}" 
                                             Margin="4,0" Padding="4" TextWrapping="Wrap" AcceptsReturn="True"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>

                <!-- 提示文字 -->
                <TextBlock Text="註:首夜順序、其他夜晚順序的編輯功能在夜晚順序編輯器中"
                           FontSize="10" Foreground="#999" Margin="0,5,0,0" 
                           TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按鈕 -->
        <Border Grid.Row="1" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,15">
                <Button Content="確定" Padding="20,8" Margin="0,0,10,0"
                        Click="OK_Click" IsDefault="True"
                        Background="#2196F3" Foreground="White" BorderThickness="0"/>
                <Button Content="取消" Padding="20,8"
                        Click="Cancel_Click" IsCancel="True"
                        Background="#999" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
